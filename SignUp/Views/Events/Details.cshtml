@model SignUp.Models.Event

@{
    ViewData["Title"] = "Details";
}

@{
    IEnumerable<dynamic> PersonClassInfo = ViewBag.person;
}
<h1>Details</h1>

<div>
    <h4>Event</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.EventName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.EventName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.EventDate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.EventDate)
        </dd>

        <dt class="col-sm-2">
            Event Host
        </dt>
        <dd class="col-sm-10">
            @foreach (var i in PersonClassInfo)
            {
                if (i.Id == Model.PersonId)
                {
                    @i.PersonName
                }
            }
        </dd>
        <dt class="col-sm-2">
            Item
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Item1)       ->


            @foreach (var i in PersonClassInfo)
            {
                if (i.Id == Int32.Parse(Model.Name1))
                {@i.PersonName}
        }
        </dd>
        <dt class="col-sm-2">
            Item
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Item2)       ->


            @foreach (var i in PersonClassInfo)
            {
                if (i.Id == Int32.Parse(Model.Name2))
                {@i.PersonName}
        }
        </dd>
        <dt class="col-sm-2">
            Item
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Item3)       ->


            @foreach (var i in PersonClassInfo)
            {
                if (i.Id == Int32.Parse(Model.Name3))
                {@i.PersonName}
        }
        </dd>
        <dt class="col-sm-2">
            Item
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Item4)       ->


            @foreach (var i in PersonClassInfo)
            {
                if (i.Id == Int32.Parse(Model.Name4))
                {@i.PersonName}
        }
        </dd>
        <dt class="col-sm-2">
            Item
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Item5)       ->


            @foreach (var i in PersonClassInfo)
            {
                if (i.Id == Int32.Parse(Model.Name5))
                {@i.PersonName}
        }
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
